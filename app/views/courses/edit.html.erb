<% title "Admin: Editing #{@course.title}" %>
<div class="container">

  <h2 class="blank-heading text-center">Editing Course</h2>

  <p class="text-center action"><%= link_to 'Show', @course, class: "btn btn-outline-primary" %> <%= link_to 'Back', courses_path, class: "btn btn-outline-primary" %>

  <div class="card">
    <div class="card-block">
      <%= render 'form', course: @course %>
    </div>
  </div>

</div>
