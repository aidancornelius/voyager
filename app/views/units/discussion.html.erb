<% title @component.title %>
<div class="container">

  <h2 class="blank-heading text-center"><%= @component.title %></h2>
  <p class="text-center"><a href="/course/<%= "#{params[:course_slug]}" %>/modules/" class="btn btn-outline-secondary">↑ Modules</a> <%= render partial: "fwdbck" %> <% if current_user.administrator? %><%= link_to "Edit Component", edit_lesson_component_path(@component), class: "btn btn-outline-danger" %><% end %></p>

  <div class="row top-pad content">
    <div class="col-12">
      <%= mdrender(@component.body).html_safe %>
    </div>
  </div>

  <% if @discussion %>
  <%= render partial: "discussion" %>
  <% end %>

  <p class="text-center"><%= render partial: "fwdbck" %></p>
  <br>

</div>
