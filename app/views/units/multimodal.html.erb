<% title @component.title %>
<div class="container">

  <h2 class="blank-heading text-xs-center"><%= @component.title %></h2>
  <p class="text-xs-center"><a href="/modules/" class="btn btn-outline-secondary">↑ Modules</a> <%= render partial: "fwdbck" %> <%= render partial: "completion" %> <% if current_user.administrator? %><%= link_to "Edit Component", edit_lesson_component_path(@component), class: "btn btn-outline-danger" %><% end %></p>

  <% if @component.feature? %>
  <div class="clearfix"></div>
  <div class="featured" style="background-image: url('<%= @component.feature.expiring_url(50, :xxlarge) %>');">
  </div>
  <% end %>
  <div class="row top-pad mainrow<% if @component.feature? %> hasfeature<% end %>">
    <div class="col-xs-12">
      <%= mdrender(@component.body).html_safe %>
    </div>
  </div>

  <p class="text-xs-center"><%= render partial: "fwdbck" %> <%= render partial: "completion" %></p>
  <%= render partial: "progress" %>

  <% if @discussion %>
  <%= render partial: "discussion" %>
  <% else %>
  <br>
  <% end %>

</div>
